1.
对文件的随机创建和删除导致文件的存储位置过于离散，者导致读取文件时需要多次寻址才能找到正确的位置，效率大大下降，此现象被称为磁盘碎片。Ext2文件系统每个文件对应的block位置存储于inode中，因此存储位置离散对性能影响也不大。碎片整理的必要性也就不大。
2.
不能
3.
19.68MB。
对不同使用特点的分区采用不同的block大小，或者打包文件。
4.
etc的inode->etc的block->passwd的inode->passwd的block。
